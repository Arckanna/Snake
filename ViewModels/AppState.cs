namespace Snake.ViewModels
{
    /// <summary>
    /// État de l'application (écran actuellement affiché).
    /// </summary>
    public enum AppState
    {
        Home,
        Playing,
        GameOver
    }
}
